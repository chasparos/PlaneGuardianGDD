# Procedural generation

Use this directory for the versioned specifications that deterministically derive cards from GUIDs, including random-stream allocation, card properties, names, abilities, lore, and visuals.
